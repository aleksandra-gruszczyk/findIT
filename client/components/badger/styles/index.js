import { hoverText } from './hoverText'
import { wrapper, img, medal } from './image'
import { badgerItem, badgerName, badgerList } from './misc'
import { votes } from './votes'

export default {
  badgerItem,
  badgerName,
  badgerList,
  hoverText,
  wrapper,
  img,
  medal,
  votes,
}